@spec solve([integer()], [integer()]) :: float()
def solve(l1, l2) do
  total = length(l1) + length(l2)
  helper(l1, l2, 0, Integer.floor_div(total, 2))
end
def helper(l1, l2, count, end) when count == end, do:
def helper(l1, l2, count, end) do
  
end
